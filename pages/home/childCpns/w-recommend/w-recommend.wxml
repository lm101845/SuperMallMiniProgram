<view class="recommend">
  <block wx:for="{{recommends}}" wx:key="index">
    <view class="recommend-item">
      <image src="{{item.image}}" class="recommend-item-image" bind:load="handleImageLoad"/>
      <view class="recommend-item-view">{{item.title}}</view>
    </view>
  </block>
</view>
